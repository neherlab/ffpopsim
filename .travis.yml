language: cpp
compiler:
  - gcc
script: make src tests

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -y gsl-bin libgsl0-dbg libgsl0-dev libgsl0ldbl libboost-all-dev swig

